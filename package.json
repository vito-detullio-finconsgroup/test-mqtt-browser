{
  "name": "test-mqtt-browser",
  "version": "0.0.1",
  "description": "test-mqtt-browser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx ws -d web -p 8080 -o --rewrite \"/test-mqtt-browser/(.*) -> /$1\""
  },
  "author": "",
  "license": " AGPL-3.0-or-later",
  "devDependencies": {
    "local-web-server": "^5.2.0"
  }
}
