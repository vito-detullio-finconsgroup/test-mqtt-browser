{
  "name": "test-mqtt-browser",
  "version": "0.0.1",
  "description": "test-mqtt-browser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx light-server -s web --servePrefix /test-mqtt-browser -p 8080"
  },
  "author": "",
  "license": " AGPL-3.0-or-later",
  "devDependencies": {
    "light-server": "^2.9.1"
  }
}
